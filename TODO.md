# TODO

- [x] Add an “About” section at the bottom of the portfolio (using `id="about"`).
- [x] Implement matching layout/typography styles in `styles.css` (same color palette, font weights, hierarchy).
- [x] Add responsive rules so the section looks consistent on mobile.
- [x] Ensure navigation link (`#about`) scrolls to the new section.
- [x] Run a quick sanity check by opening the page in browser.
- [x] Create a “Demo reel” section inside `index.html` (no extra page).






